{% extends 'base.html.twig' %}

{% block title %}Liste des villes d'un Département{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }
    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style>

<div class="container">

    <table class="table">
        <tr>
            <th>Nom</th>
            <th>Prix</th>
            <th>Editer</th>
            <th>Effacer</th>
        </tr>
        {% for ville in listeVille %}
        <tr>
            <td><a href="{{path('admin_ville_show',{'id':ville.id})}}">{{ville.name}}</a></td>
            <td>{{ville.codePostal}}</td>
            <td><a href="{{path('admin_ville_edit',{'id':ville.id})}}">Editer ville</a></td>
            <td><a href="{{path('admin_ville_delete',{'id':ville.id})}}">Supprimer ville</a></td>
        </tr>
        {% endfor  %}
    </table>

</div>
{% endblock %}